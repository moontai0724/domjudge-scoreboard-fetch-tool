/** @type {number} which row index that data start fill from (start from 1) */
const DATA_START_ROW = 2;

/** @type {number} which column index that data start fill from (start from 1) */
const DATA_START_COLUMN = 6;

/** @type {number} index of ID column (start from 1) */
const ID_COLUMN = 3;

/** @type {string} color hex that will filled when someone already passed a problem */
const ICON_PASSED = "✓";

/** @type {string} color hex that will filled when someone not passed a problem yet */
const ICON_NOT_PASSED = "⨯";

/** debug mode */
const debug = true;
